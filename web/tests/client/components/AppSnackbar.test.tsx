import React from 'react'
import { render } from '@testing-library/react'
import AppBackDrop from '../../../src/client/components/AppBackDrop'

/**
 * @jest-environment jsdom
 */

test('Test', () => {
	// empty for now
})
